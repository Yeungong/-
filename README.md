# -Agent
项目的核心痛点是日常需要频繁在邮件、即时通讯工具（如 IRC/各种社群）和待办事项软件之间手动搬运和筛选信息，导致注意力严重分散。 核心逻辑流依赖于多 Agent 协作和复杂状态机：信息摄入层 Agent 负责监听各个数据源，提取出包含明确任务意图的消息；随后交由决策 Agent 进行长链推理，判断任务的优先级、前置条件和所需资源；最后通过 API 调度 Agent 自动在待办清单中创建任务，并生成初步的执行建议草稿回复给发送方。
